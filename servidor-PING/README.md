+ Abrir terminal para o servidor e executar o mesmo <br> 
 `python3 udpping_server.py`


+ Abrir outro terminal para o cliente 
executar a linha <br>
  `python3 UDP_Ping_cli.py localhost 12000`
